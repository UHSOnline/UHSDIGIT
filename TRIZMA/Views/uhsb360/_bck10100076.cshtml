@model List<TRIZMA.Models.UHSOMD11vDb>


@foreach (var item in Model)
{
    <tr style="color:darkslategray">
        <td>@Html.DisplayFor(modelItem => item.crdate)</td>
        <td>@Html.DisplayFor(modelItem => item.distnm)</td>
        <td>@Html.DisplayFor(modelItem => item.CSTNM)</td>
        <td><a class="fa fa-share" href="/uhsb360/getEditPar10100076?dctp=1010007&id1=@item.acctid&id2=2&id3=@item.ID" data-id="@item.ID" style="font-size:x-small; color:forestgreen; transform:scale(1.5)"></a></td>
    </tr>
}

