@model List<TRIZMA.Models.k2017orgStrSubGrViewDb>


@foreach (var item in Model)
{
    <tr>
        <td>@Html.DisplayFor(modelItem => item.ID)</td>
        <td>
            <a href="@Url.Action("Edit", "k2017orgStrSubGr", new { id = item.ID })" style="color:forestgreen; padding-left:5px"><i class="fa fa-edit"></i></a>
            <a href="@Url.Action("Delete", "k2017orgStrSubGr", new { id = item.ID })" style="color:red; padding-left:8px"><i class="fa fa-power-off"></i></a>
        </td>
        <td>@Html.DisplayFor(modelItem => item.orgStrL1)</td>
        <td>@Html.DisplayFor(modelItem => item.orgStrL2)</td>
        <td>@Html.DisplayFor(modelItem => item.orgStrSubGr)</td>
        <td>@Html.DisplayFor(modelItem => item.createdDT)</td>
    </tr>
}
