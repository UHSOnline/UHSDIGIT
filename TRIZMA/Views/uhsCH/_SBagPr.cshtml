@model List<TRIZMA.Models.agentsTODistDb>

@foreach (var item in Model)
{
    <li style="padding-left:3px">
        <a href="@Url.Action("Index", "formsCRUD", new { projectID = item.projectID, taskOrderID = item.taskOrderID, Int1 = 1 })"><span style="padding-left:5px">@item.taskOrder</span></a>
        @*<ul class="nav nav-second-level">
            @Html.Action("SBagTo", "uhsCH", new { id = item.projectID })
        </ul>*@
    </li>
}
